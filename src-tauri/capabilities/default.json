{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Aria main window — core permissions only, no shell/fs access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
